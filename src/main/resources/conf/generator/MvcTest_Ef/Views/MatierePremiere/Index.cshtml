@model IEnumerable<MvcTest_Ef.Models.MatierePremiere>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Designation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrixUnitaire)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Designation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrixUnitaire)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMatierePremiere">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMatierePremiere">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMatierePremiere">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
